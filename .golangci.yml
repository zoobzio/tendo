version: "2"

run:
  timeout: 5m
  tests: true
  build-tags:
    - integration

linters:
  enable:
    # Security-critical linters
    - gosec        # Comprehensive security analyzer
    - errorlint    # Proper error handling patterns
    - noctx        # HTTP requests must have context
    - bodyclose    # HTTP response body must be closed
    - sqlclosecheck # SQL rows/statements must be closed

    # Code quality with security implications
    - govet        # Suspicious constructs
    - ineffassign  # Detect ineffectual assignments
    - staticcheck  # Advanced static analysis
    - unused       # Unused code detection

    # Error handling
    - errcheck     # Unchecked errors
    - errchkjson   # JSON marshaling errors
    - wastedassign # Wasted assignments

    # Best practices
    - gocritic     # Opinionated linter
    - revive       # Fast, configurable linter
    - unconvert    # Unnecessary type conversions
    - dupl         # Code duplication
    - goconst      # Repeated strings as constants
    - godot        # Comment sentences
    - godox        # TODO/FIXME comments
    - misspell     # Spelling mistakes
    - nakedret     # Naked returns
    - prealloc     # Slice capacity hints
    - predeclared  # Shadowing predeclared identifiers
    - copyloopvar  # Loop variable capture

  exclusions:
    rules:
      # Allow TODO comments in development
      - linters:
          - godox
        text: "TODO|FIXME|BUG"

      # Test files can have longer functions and repeated strings
      - path: _test\.go
        linters:
          - funlen
          - goconst
          - dupl
          - govet
          - revive
          - errcheck

      # Float16 conversions are intentionally bounded by IEEE 754 format
      - path: float16\.go
        linters:
          - gosec
        text: "G115"

      # ML code uses math/rand intentionally (not crypto-sensitive)
      - path: cpu/
        linters:
          - gosec
        text: "G404"

      # CPU backend methods implement tendo.Backend interface (docs on interface)
      - path: cpu/
        linters:
          - revive
        text: "exported:"

      # ifElseChain is stylistic and switch is not always clearer
      - linters:
          - gocritic
        text: "ifElseChain"

  settings:
    gosec:
      severity: medium
      confidence: medium
      excludes:
        - G104  # We'll use errcheck instead
      config:
        global:
          audit: true

    govet:
      enable-all: true
      disable:
        - printf  # Bug in golang.org/x/tools@v0.39.0 panics on CGO deps (github.com/daulet/tokenizers)

    errcheck:
      check-type-assertions: false  # Type assertions should panic on failure (programming errors)
      check-blank: true
      exclude-functions:
        - (io.Closer).Close  # Handled by bodyclose

    gocritic:
      enabled-checks:
        - commentedOutCode
        - deferInLoop
        - emptyFallthrough
        - emptyStringTest
        - equalFold
        - evalOrder
        - exposedSyncMutex
        - externalErrorReassign
        - filepathJoin
        - hexLiteral
        - httpNoBody
        - importShadow
        - indexAlloc
        - initClause
        # - nestingReduce  # Stylistic, not always clearer
        - nilValReturn
        - octalLiteral
        - preferFilepathJoin
        - preferFprint
        - preferStringWriter
        - preferWriteByte
        - rangeExprCopy
        - rangeValCopy
        - redundantSprint
        - regexpPattern
        - regexpSimplify
        - ruleguard
        - sloppyReassign
        - sortSlice
        - sprintfQuotedString
        - sqlQuery
        - stringConcatSimplify
        - stringXbytes
        - syncMapLoadAndDelete
        - truncateCmp
        # - typeAssertChain  # Interface polymorphism is common in tensor libs
        - typeDefFirst
        - typeUnparen
        - unlabelStmt
        # - unnamedResult  # Not always beneficial for multi-return funcs
        - unnecessaryBlock
        - unnecessaryDefer
        - weakCond
        - yodaStyleExpr

    revive:
      severity: warning
      rules:
        - name: blank-imports
        # - name: context-as-argument  # API compatibility - changing arg order is breaking
        - name: context-keys-type
        - name: dot-imports
        - name: empty-block
        - name: error-naming
        - name: error-return
        - name: error-strings
        - name: errorf
        - name: exported
        - name: if-return
        - name: increment-decrement
        - name: indent-error-flow
        - name: modifies-parameter
        - name: modifies-value-receiver
        - name: package-comments
        - name: range
        - name: receiver-naming
        # - name: redefines-builtin-id  # Common parameter names like min/max
        - name: string-of-int
        - name: superfluous-else
        - name: time-naming
        - name: unexported-return
        - name: unnecessary-stmt
        - name: unreachable-code
        # - name: unused-parameter  # Interface implementations require matching signatures
        # - name: unused-receiver  # Interface implementations require receivers
        - name: useless-break
        - name: var-declaration
        - name: var-naming

    dupl:
      threshold: 150

    goconst:
      min-len: 3
      min-occurrences: 3

    misspell:
      locale: US

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  new: false
